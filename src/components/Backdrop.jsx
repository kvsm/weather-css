import React from 'react'
import classnames from 'classnames'

const Backdrop = (props) => {
  const classes = classnames('wc-backdrop')
  return (
    <div className={classes}></div>
  )
}

export default Backdrop
